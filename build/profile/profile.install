<?php

/**
 * @file
 * Install, update and uninstall for the PROFILE_SLUG  profile.
 */

/**
 * Implements hook_install().
 *
 * Perform actions to set up the site for PROFILE_SLUG .
 *
 * @see system_install()
 */
function PROFILE_SLUG_install() {
  // Set our default theme
  \Drupal::configFactory()
  ->getEditable('system.theme')
  ->set('default', 'newspapers_lib_unb_ca')
  ->save();

  // Can add code in here to make nodes, terms, etc.
}
