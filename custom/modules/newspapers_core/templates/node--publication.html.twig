{#
/**
 * @file
 * Default theme implementation to display a node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - node.getCreatedTime() will return the node creation timestamp.
 *   - node.hasField('field_example') returns TRUE if the node bundle includes
 *     field_example. (This does not indicate the presence of a value in this
 *     field.)
 *   - node.isPublished() will return whether the node is published or not.
 *   Calling other methods, such as node.delete(), will result in an exception.
 *   See \Drupal\node\Entity\Node for a full list of public properties and
 *   methods for the node object.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - author_picture: The node author user entity, rendered using the "compact"
 *   view mode.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - author_name: Themed author name field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - node: The current template type (also known as a "theming hook").
 *   - node--type-[type]: The current node type. For example, if the node is an
 *     "Article" it would result in "node--type-article". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - node--view-mode-[view_mode]: The View Mode of the node; for example, a
 *     teaser would result in: "node--view-mode-teaser", and
 *     full: "node--view-mode-full".
 *   The following are controlled through the node publishing options.
 *   - node--promoted: Appears on nodes promoted to the front page.
 *   - node--sticky: Appears on nodes ordered above other non-sticky nodes in
 *     teaser listings.
 *   - node--unpublished: Appears on unpublished nodes visible only to site
 *     admins.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - content_attributes: Same as attributes, except applied to the main
 *   content tag that appears in the template.
 * - author_attributes: Same as attributes, except applied to the author of
 *   the node tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - teaser: Flag for the teaser state. Will be true if view_mode is 'teaser'.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 * - readmore: Flag for more state. Will be true if the teaser content of the
 *   node cannot hold the main body content.
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_node()
 *
 * @ingroup themeable
 */
#}
{{ attach_library('newspapers_core/copy_citation') }}
{{ attach_library('newspapers_core/holding-display') }}
{% set holdingSoftLimit = 5 %}

{% if back_link %}
  <div class="mb-3">
    {{ back_link }}
  </div>
{% endif %}

{% if view_mode == 'featured' %}
  <div{{ attributes.addClass("featured-publication d-flex flex-column flex-sm-row") }}>
    <div class="mr-3">
      <a href="{{ url }}" tabindex="-1">
      {% if content.field_sample_image|field_value %}
        <figure class="d-inline-block">{{ content.field_sample_image }}</figure>
      {% else %}
        <figure class="d-inline-block"><img class="no-borders image-placeholder"
        src="/{{ active_theme_path() }}/src/img/sample-publication.jpg" alt="" /></figure>
      {% endif %}
      </a>
    </div>
    <div>
      <h2>Featured Publication</h2>
      <div class="title"><strong><a href="{{ url }}">{{ label }}</a></strong></div>
      {% if publication_date_value %}
        <div class="date">{{ publication_date_value }}</div>
      {% endif %}
      {% if content.field_place_of_publication|field_value %}
        <div class="place">{{ content.field_place_of_publication|field_value }}</div>
      {% endif %}
      {% if frequency_value %}
        <div class="frequency">{{ frequency_value }}</div>
      {% endif %}
      {% if content.field_description_html|field_value %}
        <div class="description mt-2">{{ content.field_description_html|field_value }}</div>
      {% elseif content.field_description|field_value %}
        <div class="description mt-2">{{ content.field_description|field_value }}</div>
      {% endif %}
    </div>
  </div>
{% else %}
  <div{{ attributes.addClass("no-gutters") }}>
    <div class="row">
      <section class="publication col-lg-8 mb-4">
        {{ title_prefix }}
        {% if not page %}
          <h2{{ title_attributes }}>
            <a href="{{ url }}" rel="bookmark">{{ label }}</a>
          </h2>
        {% endif %}
        {{ title_suffix }}

        <div class="title-details">
          <h2 class="bg-unb-light border p-2">{% trans %}Details{% endtrans %}</h2>
          {% if content.field_sample_image|field_value %}
            <figure class="image-link-shadow">
              {{ content.field_sample_image }}
              <figcaption class="sr-only">
                Sample image of <i>{{ label }}</i>
              </figcaption>
            </figure>
          {% else %}
            <figure>
              <img class="no-borders image-placeholder"
              src="/{{ active_theme_path() }}/src/img/sample-publication.jpg" alt="" />
            </figure>
          {% endif %}

          <dl class="pl-2">
            {% if content.field_subtitle|field_value %}
              <dt>{% trans %}Subtitle / Tagline{% endtrans %}:</dt>
              <dd>{{ content.field_subtitle|field_value }}</dd>
            {% endif %}

            {% if publication_date_value %}
              <dt>{{ publication_date_label }}:</dt>
              <dd>{{ publication_date_value }}</dd>
            {% endif %}

            {% if content.field_place_of_publication|field_value %}
              <dt>{{ content.field_place_of_publication|field_label }}:</dt>
              <dd>{{ content.field_place_of_publication|field_value }}</dd>
            {% endif %}

            {% if frequency_value %}
              <dt>{{ frequency_label }}:</dt>
              <dd>{{ frequency_value }}</dd>
            {% endif %}

            {% if content.field_geographic_coverage|field_value %}
              <dt>{{ content.field_geographic_coverage|field_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for key, item in content.field_geographic_coverage if key|first != '#' %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if earlier_titles_up %}
              {% set count = content.field_serial_relation_pre_ref_up|field_value|length %}
              {% if count > 1 %}<dt>{% trans %}Earlier titles{% endtrans %}:</dt>
              {% else %}<dt>{% trans %}Earlier title{% endtrans %}:</dt>
              {% endif %}
              <dd>{{ content.field_serial_relationship_op_pre|field_value }} {{ earlier_titles_up }}</dd>
            {% endif %}

            {% if later_titles_up and later_titles_down %}
              <dt>{% trans %}Later title{% endtrans %}:</dt>
              <dd>{% trans %}Merged with{% endtrans %}
                {{ later_titles_up }}{% trans %}to form{% endtrans %}
                {{ later_titles_down }}</dd>
            {% elseif later_titles_down %}
              {% set count = content.field_serial_relation_suc_ref_dn|field_value|length %}
              {% if count > 1 %}<dt>{% trans %}Later titles{% endtrans %}:</dt>
              {% else %}<dt>{% trans %}Later title{% endtrans %}:</dt>
              {% endif %}
              <dd>{{ content.field_serial_relationship_op_suc|field_value }} {{ later_titles_down }}</dd>
            {% endif %}

            {% if content.field_title_notes|field_value %}
              <dt>{{ content.field_title_notes|field_label }}:</dt>
              <dd>{{ content.field_title_notes|field_value }}</dd>
            {% endif %}

            {% if family %}
              <dt class="title-history">
                <hr />
                {% trans %}Broad Title History{% endtrans %}:
              </dt>
              <dd class="no-borders">({% trans %}This includes all related titles and supplements and
                the publication dates of each{%  endtrans %})
                <br >
                {% set supplemental_tid = node.field_family.target_id %}
                {% if supplemental_tid %}
                  {{ drupal_field('field_supplemental_information', 'taxonomy_term', supplemental_tid) }}
                {% endif %}
                {{ family }}
              </dd>

              {% if supplemental_title %}
                <dt>{{ supplemental_title_label }}:</dt>
                <dd>
                  {{ supplemental_title }}
                </dd>
              {% endif %}
              <dd><hr /></dd>
            {% endif %}

            {% if publisher_values %}
              <dt>{{ publisher_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for item in publisher_values %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if editor_values %}
              <dt>{{ editor_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for item in editor_values %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if content.field_description_html|field_value or content.field_description_file|field_value %}
              <dt>{% trans %}Description{% endtrans %}:</dt>
              <dd>
                {% if content.field_description_html|field_value %}
                  <div class="description-html">{{ content.field_description_html|field_value }}</div>
                {% endif %}
                {% if content.field_description_file|field_value %}
                  <div class="description-files">{{ content.field_description_file|field_value }}</div>
                {% endif %}
              </dd>
            {% elseif content.field_description|field_value %}
              <dt>{{ content.field_description|field_label }}:</dt>
              <dd>{{ content.field_description|field_value }}</dd>
            {% endif %}

            {% if content.field_subjects|field_value %}
              <dt>{{ content.field_subjects|field_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for key, item in content.field_subjects if key|first != '#' %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if content.field_subject_notes|field_value %}
              <dt>{{ content.field_subject_notes|field_label }}:</dt>
              <dd>{{ content.field_subject_notes|field_value }}</dd>
            {% endif %}

            {% if content.field_oclc|field_value %}
              <dt>{{ content.field_oclc|field_label }}:</dt>
              <dd>{{ content.field_oclc|field_value }}</dd>
            {% endif %}

            {% if content.field_issn|field_value %}
              <dt>{{ content.field_issn|field_label }}:</dt>
              <dd>{{ content.field_issn|field_value }}</dd>
            {% endif %}

            {% if content.field_microfilmed_by|field_value %}
              <dt>{{ content.field_microfilmed_by|field_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for item in microfilmed_by_values %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if language_values %}
              <dt>{{ language_label }}:</dt>
              <dd>
                <ul class="list-unstyled">
                  {% for item in language_values %}
                    <li>{{ item }}</li>
                  {% endfor %}
                </ul>
              </dd>
            {% endif %}

            {% if content.field_supplemental_information|field_value %}
              <dt>{{ content.field_supplemental_information|field_label }}:</dt>
              <dd class="no-borders">{{ content.field_supplemental_information|field_value }}</dd>
            {% endif %}
          </dl>

          {% if citation %}
            {{ citation }}
          {% endif %}
        </div>

        <hr>
        <div class="d-flex flex-column">
          {% if display_report_link %}
          <div class="mb-1 link-report">
            <a href="{{ report_url }}">
              Report additional information or errors<span class="fa-solid fa-pen-to-square fa-sm ml-1" aria-hidden="true"></span>
            </a>
          </div>
          {% endif %}
          <div class="link-external-terms">
            <a href="https://lib.unb.ca/archives/policies/terms">
              Terms of Use for the NBHNP<span class="fa-solid fa-external-link-alt fa-sm ml-1"
              aria-description="(link leads to external site)"></span>
            </a>
          </div>
        </div>
      </section>

      <aside class="col-lg-4 mb-4 publication">
        {% set missing_coverage_label = 'Missing Coverage' %}
        {% set institution_label = 'Institution' %}

        <div class="title-holdings">
          <h2 class="bg-unb-light border p-2">{% trans %}Holdings{% endtrans %}</h2>
          {% if holding|length > 0 %}
            {% if holding.online|length > 0 or holding.digital|length > 0 %}
              <div class="holding-group pl-2">
                <h3>{% trans %}Online{% endtrans %}:</h3>
                <ul class="holding-type">
                  {% for item in holding.digital %}
                    {% set digitalcount = ( digitalcount | default(0) ) + 1 %}
                    <li class="holding-item">
                      {% for field in item %}
                        {% if field.value %}
                          {% set className = 'field-' ~ field.label|clean_class %}
                          {% if field.label == missing_coverage_label %}
                            <div class="{{ className }}">
                              <button class="btn btn-link p-0"
                              data-target="#missingDigital{{ digitalcount }}" data-toggle="modal" type="button">
                               {{ missing_coverage_label }}<span class="fa-solid fa-circle-info fa-sm ml-1"
                                aria-description="(button opens a modal window)"></span>
                              </button>
                              <div id="missingDigital{{ digitalcount }}" class="modal fade"
                              aria-hidden="true" aria-labelledby="digital{{ digitalcount }}ModalLabel" tabindex="-1">
                                <div class="modal-dialog modal-xl">
                                  <div class="modal-content">
                                    <div class="modal-header">
                                      <div id="digital{{ digitalcount }}ModalLabel" class="modal-title h3">
                                        {{ field.label }}
                                      </div>
                                      <button class="btn btn-primary" aria-label="Close" data-dismiss="modal" type="button">
                                        Close<span class="fa-solid fa-window-close ml-1" aria-hidden="true"></span>
                                        <span class="sr-only">Modal</span>
                                      </button>
                                    </div>
                                    <div class="modal-body">
                                      {{ field.value|raw }}
                                    </div>
                                  </div>
                                </div>
                              </div>
                            </div>
                          {% else %}
                            <div class="{{ className }}">{{ field.value|raw }}</div>
                          {% endif %}
                        {% endif %}
                      {% endfor %}
                    </li>
                  {% endfor %}
                  {% for item in holding.online %}
                  {% set onlinecount = ( onlinecount | default(0) ) + 1 %}
                  <li class="holding-item">
                    {% for field in item %}
                      {% if field.value %}
                        {% set className = 'field-' ~ field.label|clean_class %}
                        {% if field.label == missing_coverage_label %}
                          <div class="{{ className }}">
                            <button class="btn btn-link p-0"
                                    data-target="#missingOnline{{ onlinecount }}" data-toggle="modal" type="button">
                              {{ missing_coverage_label }}<span class="fa-solid fa-circle-info fa-sm ml-1"
                                 aria-description="(button opens a modal window)"></span>
                            </button>
                            <div id="missingOnline{{ onlinecount }}" class="modal fade"
                                 aria-hidden="true" aria-labelledby="online{{ onlinecount }}ModalLabel" tabindex="-1">
                              <div class="modal-dialog modal-xl">
                                <div class="modal-content">
                                  <div class="modal-header">
                                    <div id="online{{ onlinecount }}ModalLabel" class="modal-title h3">
                                      {{ field.label }}
                                    </div>
                                    <button class="btn btn-primary" aria-label="Close" data-dismiss="modal" type="button">
                                      Close<span class="fa-solid fa-window-close ml-1" aria-hidden="true"></span>
                                      <span class="sr-only">Modal</span>
                                    </button>
                                  </div>
                                  <div class="modal-body">
                                    {{ field.value|raw }}
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        {% else %}
                          <div class="{{ className }}">{{ field.value|raw }}</div>
                        {% endif %}
                      {% endif %}
                    {% endfor %}
                  </li>
                  {% endfor %}
                </ul>
              </div>
            {% endif %}

            {% if holding.microform|length > 0 %}
              <div class="holding-group holding-group-microform pl-2">
                {% set microformClasses = ['holding-item'] %}
                {% set microformCollapsed = [] %}
                {% set microformCount = 0 %}
                <h3>{% trans %}Microform{% endtrans %}:</h3>
                <ul class="holding-type">
                  {% for item in holding.microform %}
                    {% set microformCount = microformCount + 1 %}
                    {% if microformCount == holdingSoftLimit + 1 %}
                      {% set microformClasses = microformClasses|merge(['collapse', 'multi-collapse']) %}
                    {% endif %}
                    {% if microformCount > holdingSoftLimit %}
                      {% set microformCollapsed = microformCollapsed|merge(["microformCount#{microformCount}"]) %}
                    {% endif %}
                    <li id="microformHolding{{ microformCount }}" class="{{ microformClasses|join(' ') }}">
                      {% for institution in item %}
                        {% set isFirstInstitutionHolding = loop.first %}
                        {% for key,field in institution %}
                          {% if field.value %}
                            {% set className = 'field-' ~ field.label|clean_class %}
                            {% if field.label == institution_label %}
                              {% if isFirstInstitutionHolding %}
                                <div class="{{ className }}">{{ field.value|raw }}</div>
                              {% else %}
                                <hr class="bg-light my-2">
                              {% endif %}
                            {% elseif field.label == missing_coverage_label %}
                              <div class="{{ className }}">
                                <button class="btn btn-link p-0 text-left"
                                data-target="#missingMicroform{{ microformcount }}" data-toggle="modal" type="button">
                                  {{ missing_coverage_label }}<span class="fa-solid fa-circle-info fa-sm ml-1"
                                     aria-description="(button opens a modal window)"></span>
                                </button>
                                <div id="missingMicroform{{ microformcount }}" class="modal fade" aria-hidden="true"
                                aria-labelledby="microform{{ microformcount }}ModalLabel" role="dialog" tabindex="-1">
                                  <div class="modal-dialog modal-xl">
                                    <div class="modal-content">
                                      <div class="modal-header">
                                        <div id="microform{{ microformcount }}ModalLabel" class="modal-title h3">
                                          {{ field.label }}
                                        </div>
                                        <button class="btn btn-primary" aria-label="Close" data-dismiss="modal" type="button">
                                          Close<span class="fa-solid fa-window-close ml-1" aria-hidden="true"></span>
                                          <span class="sr-only">Modal</span>
                                        </button>
                                      </div>
                                      <div class="modal-body">
                                        {{ field.value|raw }}
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </div>
                            {% else %}
                              <div class="{{ className }}">{{ field.value|raw }}</div>
                            {% endif %}
                          {% endif %}
                        {% endfor %}
                      {% endfor %}
                    </li>
                  {% endfor %}
                </ul>
                {% if microformCount > holdingSoftLimit %}
                  <button class="btn btn-link"
                  aria-controls="{% for item in microformCollapsed %}{{ item }}{% endfor %}" aria-expanded="false"
                  data-target=".holding-group-microform .multi-collapse" data-toggle="collapse" type="button">
                    Show All
                    <span class="sr-only">Microform Holdings</span>
                  </button>
                {% endif %}
              </div>
            {% endif %}

            {% if holding.print|length > 0 %}
              <div class="holding-group holding-group-print pl-2">
                {% set printClasses = ['holding-item'] %}
                {% set printCollapsed = [] %}
                {% set printCount = 0 %}
                <h3>{% trans %}Print{% endtrans %}:</h3>
                <ul class="holding-type">
                  {% for item in holding.print %}
                    {% set printCount = printCount + 1 %}
                    {% if printCount == holdingSoftLimit + 1 %}
                      {% set printClasses = printClasses|merge(['collapse', 'multi-collapse']) %}
                    {% endif %}
                    {% if printCount > holdingSoftLimit %}
                      {% set printCollapsed = printCollapsed|merge(["printCount#{printCount}"]) %}
                    {% endif %}
                    <li id="printHolding{{ printCount }}" class="{{ printClasses|join(' ') }}">
                      {% for institution in item %}
                        {% set isFirstInstitutionHolding = loop.first %}
                        {% for key,field in institution %}
                          {% if field.value %}
                            {% set className = 'field-' ~ field.label|clean_class %}
                            {% if field.label == institution_label %}
                              {% if isFirstInstitutionHolding %}
                                <div class="{{ className }}">{{ field.value|raw }}</div>
                              {% else %}
                                <hr class="bg-light my-2">
                              {% endif %}
                            {% elseif field.label == missing_coverage_label %}
                              {# Coverage Statement is required+lighter weight so this should display below it #}
                              <div class="{{ className }}">
                                <button class="btn btn-link p-0 text-left"
                                data-target="#missingPrint{{ printCount }}" data-toggle="modal" type="button">
                                  {{ missing_coverage_label }}<span class="fa-solid fa-circle-info fa-sm ml-1"
                                  aria-description="(button opens a modal window)"></span>
                                </button>
                                <div id="missingPrint{{ printCount }}" class="modal fade"
                                aria-labelledby="print{{ printCount }}ModalLabel" aria-hidden="true" tabindex="-1">
                                  <div class="modal-dialog modal-xl">
                                    <div class="modal-content">
                                      <div class="modal-header">
                                        <div id="print{{ printCount }}ModalLabel" class="modal-title h3">
                                          {{ field.label }}
                                        </div>
                                        <button class="btn btn-primary" aria-label="Close" data-dismiss="modal" type="button">
                                          Close<span class="fa-solid fa-window-close ml-1" aria-hidden="true"></span>
                                          <span class="sr-only">Modal</span>
                                        </button>
                                      </div>
                                      <div class="modal-body">
                                        {{ field.value|raw }}
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </div>
                            {% else %}
                              <div class="{{ className }}">{{ field.value|raw }}</div>
                            {% endif %}
                          {% endif %}
                        {% endfor %}
                      {% endfor %}
                    </li>
                  {% endfor %}
                </ul>
                {% if printCount > holdingSoftLimit %}
                  <button class="btn btn-link"
                  aria-controls="{% for item in printCollapsed %}{{ item }}{% endfor %}" aria-expanded="false"
                  data-target=".holding-group-print .multi-collapse" data-toggle="collapse" type="button">
                    Show All
                    <span class="sr-only">Print Holdings</span>
                  </button>
                {% endif %}
              </div>
            {% endif %}
          {% else %}
            <p><span class="fa-solid fa-info-circle text-muted" aria-hidden="true"></span>
              There are currently <strong>no verified holdings</strong> for this publication.
            </p>
          {% endif %}
          <div class="card text-small">
            <div class="card-body">
              <p class="card-text">A collaborative project between
                <abbr title="University of New Brunswick">UNB</abbr>,
                <abbr title="Provincial Archives of New Brunswick">PANB</abbr> and
                <abbr title="Council of Archives New Brunswick">CANB</abbr>
                is currently underway to verify all New Brunswick newspaper holdings.
                <a href="/project">This project</a>, while comprehensive, is not anticipated to be completed until 2024.
              </p>
            </div>
          </div>
        </div>
      </aside>
    </div>
  </div>
{% endif %}
