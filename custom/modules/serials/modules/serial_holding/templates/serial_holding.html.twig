{#
/**
 * @file serial_holding.html.twig
 * Default theme implementation to present Serial holding data.
 *
 * This template is used when viewing Serial holding pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_serial_holding()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('serial_holding') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
